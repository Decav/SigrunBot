app = "sigrun-bot"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

[processes]
  app = "node index.js"

[experimental]
  auto_start_machines = true
  auto_stop_machines = false
